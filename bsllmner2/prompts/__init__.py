"""Prompt and JSON-schema builders for LLM calls.

The submodules in this package build the messages and JSON schemas that are
fed to the LLM, separated from the orchestration code that issues the calls.
"""
